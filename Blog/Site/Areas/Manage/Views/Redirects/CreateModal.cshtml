@model StaticVoid.Blog.Site.Areas.Manage.Models.RedirectModel

@{
    ViewBag.Title = "Add Redirect";
}

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Add Redirect</h3>
</div>


@using (Html.BeginForm("Create", "Redirects", FormMethod.Post, new { @class = "modal-form" }))
{
    <div class="modal-body">
        @Html.ValidationSummary(true)

        <div>
            @Html.LabelFor(m => m.From)
            @Html.TextBoxFor(m => m.From)
        </div>

        <div>
            @Html.LabelFor(m => m.To)
            @Html.TextBoxFor(m => m.To)
        </div>
        <div>
            @Html.LabelFor(m => m.Temporary)
            @Html.CheckBoxFor(m => m.Temporary)
        </div>
    </div>
    
    <div class="modal-footer">
        <button class="btn" data-dismiss="modal" aria-hidden="true">Cancel</button>
        <button class="btn btn-success" type="submit">Add Redirect</button>
    </div>
}
