<ul class="nav pull-right">
    @if (Request.IsAuthenticated)
    {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <img src=" @(SecurityHelper.CurrentUser.GravatarUrl)?s=20" />
                @SecurityHelper.CurrentUser.Nickname
                <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Dashboard", "Index","Dashboard",new { Area = "Authors" },new{})</li>
                <li>@Html.ActionLink("Author Posts", "Index","PostAuthoring",new { Area = "Authors" },new{})</li>
                <li class="divider"></li>
                <li>@Html.ActionLink("Sign out", "LogOff", "Account")</li>
            </ul>
        </li>
    }
</ul>
